---
- hosts: all
  gather_facts: no

  tasks:
  - name: loggly monitor
    tags: cron
    cron:
      name="loggly monitor"
      minute="*/2"
      job="/usr/bin/logger -t `/bin/hostname` `/bin/date -u '+%Y-%m-%d %H:%M:%S'`"  
      state=absent
