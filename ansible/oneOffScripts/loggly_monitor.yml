---
- hosts: all
  gather_facts: no

  tasks:
  - name: loggly monitor
    sudo: yes
    tags: cron
    cron:
      name="loggly monitor"
      minute="*/2"
      job="logger `hostname` `date -u '+%Y-%m-%d %H:%M:%S'`"  
