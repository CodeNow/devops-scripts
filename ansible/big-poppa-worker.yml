---
- hosts: rabbitmq
- hosts: big-poppa
- hosts: consul
- hosts: mongodb
- hosts: github-varnish

- hosts: builder
  vars_files:
    - group_vars/alpha-big-poppa-worker.yml
  roles:
  - { role: notify, rollbar_token: "{{ big_poppa_worker_rollbar_token }}", tags: [ notify ] }
  - { role: builder }
  - { role: push }

- include: aws/big-poppa-worker.yml
- include: deploy/big-poppa-worker.yml
