---
- hosts: redis
- hosts: rabbitmq
- hosts: consul

- hosts: builder
  vars_files:
    - group_vars/alpha-drake-http.yml
  roles:
  - { role: notify, rollbar_token: "{{ drake_http_rollbar_token }}" }
  - { role: builder }

- include: aws/drake-http.yml
- include: deploy/drake-http.yml
