---
- hosts: redis
- hosts: consul

- hosts: fluffy
  vars_files:
    - group_vars/alpha-fluffy.yml
  roles:
  - role: notify
    rollbar_token: "{{ fluffy_rollbar_token }}"
    tags: [ notify ]
  - { role: redis_key, tags: [ setup, redis_key ] }
  - { role: builder, tags: [ build ] }
  - { role: container_start }
