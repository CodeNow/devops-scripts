---
- hosts: registry
  tasks:
    - name: gather facts about the registry
      setup: filter=*
- hosts: redis
  tasks:
    - name: gather facts about redis
      setup: filter=*
- hosts: mongodb
  tasks:
    - name: gather facts about mongo
      setup: filter=*

- hosts: cayley
  roles:
  - { role: notify, tags: "notify" }
  - { role: redis_key, tags: ["setup", "redis_key"] }
  - { role: cayley }
  - { role: container_start, tags: "deploy" }
