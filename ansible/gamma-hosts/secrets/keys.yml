apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ name }}-keys
data:
  {{ kube_config_name }}: |
    {{ lookup('file', 'secrets/kartographer/gamma.config') | indent(4) }}
  {{ kube_config_name }}: |
    {{ lookup('file', 'secrets/kartographer/gamma.config') | indent(4) }}
