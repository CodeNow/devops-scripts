---
- hosts: consul
- hosts: mongodb
- hosts: redis
- hosts: rabbitmq
- hosts: big-poppa
- hosts: socket-server-proxy
- hosts: github-varnish

- hosts: builder
  vars_files:
  - group_vars/alpha-eru.yml
  roles:
  - { role: notify, tags: [ notify ] }
  - { role: builder, tags: [ build ] }
  - { role: push, tags [ push ] }

- include: aws/eru.yml
  tags: [ lc ]
- include: deploy/eru.yml
    tags: [ deploy ]
