---
- hosts: drake
  vars_files:
    - ../group_vars/alpha-drake-http.yml
  roles:
  - role: ../roles/datadog
    tags: [ datadog ]
         
  - role: ../roles/pull_start
    number_of_containers: 1
