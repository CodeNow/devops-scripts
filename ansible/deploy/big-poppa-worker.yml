---
- hosts: big-poppa
  vars_files:
    - ../group_vars/alpha-big-poppa-worker.yml
  roles:
  - role: ../roles/datadog
    tags: [ datadog ]
         
  - role: ../roles/pull_start
    number_of_containers: 1
