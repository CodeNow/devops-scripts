---
- hosts: detention
  vars_files:
    - ../group_vars/alpha-detention.yml
  roles:
  - role: ../roles/datadog
    tags: [ datadog ]
         
  - role: ../roles/pull_start
    number_of_containers: 1
