---
- hosts: github-varnish
  vars_files:
    - ../group_vars/alpha-github-varnish.yml
  roles:
  - role: ../roles/datadog
    tags: [ datadog ]
         
  - role: ../roles/pull_start
    number_of_containers: 1
