---
- hosts: api
  vars_files:
    - group_vars/alpha-api-base.yml
    - group_vars/alpha-socket-server.yml
  roles:
  - role: datadog
    tags: [ datadog ]
          
  - role: pull_kill_start
    number_of_containers: 1
