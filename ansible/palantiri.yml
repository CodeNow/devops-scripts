---
- hosts: rabbitmq
- hosts: consul
- hosts: swarm-manager
- hosts: palantiri

- hosts: builder
  vars_files:
    - group_vars/alpha-palantiri.yml
  roles:
  - { role: notify, rollbar_token: "{{ palantiri_rollbar_token }}" }
  - { role: docker_client }
  - { role: builder }
  - { role: push }

- include: aws/palantiri.yml
- include: deploy/palantiri.yml
