---
- hosts: redis

- hosts: navi
  vars_files:
    - "group_vars/navi.yml"
  roles:
  - { role: notify, tags: "notify" }
  - { role: builder, tags: "build" }
  - { role: container_start, tags: "deploy" }
