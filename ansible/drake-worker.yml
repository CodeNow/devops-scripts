---
- hosts: rabbitmq
- hosts: consul

- hosts: builder
  vars_files:
    - group_vars/alpha-drake-worker.yml
  roles:
  - { role: notify, rollbar_token: "{{ drake_worker_rollbar_token }}" }
  - { role: builder }

- include: aws/drake-worker.yml
- include: deploy/drake-worker.yml
