---
- hosts: registry

- hosts: kibana
  vars_files:
    - "group_vars/alpha-kibana.yml"
  roles:
  - { role: builder, tags: "build" }
  - { role: kibana }
  - { role: container_kill_start, tags: deploy }
