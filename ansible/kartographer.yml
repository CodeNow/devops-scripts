---
- hosts: kartographer
  vars_files:
    - group_vars/alpha-kartographer.yml
  roles:
  - role: notify
    rollbar_token: "{{ kartographer_rollbar_token }}"

  - role: build_with_dockerfile

  - role: kartographer

  - role: k8-deployment
