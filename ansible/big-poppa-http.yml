---
- hosts: rabbitmq
- hosts: cream_group
- hosts: consul
- hosts: mongodb
- hosts: github-varnish
- hosts: big-poppa

- hosts: builder
  vars_files:
    - group_vars/alpha-big-poppa-http.yml
  roles:
  - { role: notify, rollbar_token: "{{ big_poppa_http_rollbar_token }}" }
  - { role: builder }
  - { role: push }

- include: aws/big-poppa-http.yml
- include: deploy/big-poppa-http.yml
