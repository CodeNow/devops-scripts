- name: create AWS SG
  ec2_group:
    name: "{{ sg_name }}"
    description: "{{ env }} security policy for {{ descr }}"
    vpc_id: "{{ vpc_id }}"
    region: "{{ region }}"
    aws_secret_key: "{{ aws_secret_key }}"
    aws_access_key: "{{ aws_access_key }}"
