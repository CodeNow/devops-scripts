---
- name: create hipache config directory
  sudo: yes
  file:
    path=/opt/hipache-conf
    state=directory

- name: copy configs
  sudo: yes
  template:
    src=config.json
    dest=/opt/hipache-conf
