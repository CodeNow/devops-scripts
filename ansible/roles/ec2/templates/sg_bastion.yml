---
  SecurityGroups: 
    - 
      IpPermissionsEgress: 
        - 
          IpProtocol: "-1"
          IpRanges: 
            - 
              CidrIp: "0.0.0.0/0"
          UserIdGroupPairs: []
          PrefixListIds: []
      Description: "{{ env }} Bastion Security Policy"
      Tags: 
        - 
          Value: "{{ env }}-bastion"
          Key: "Name"
        -   
          Value: "bastion"
          Key: "Service"
        -   
          Value: "{{ env }}"
          Key: "Environment"
      IpPermissions: 
        - 
          PrefixListIds: []
          FromPort: 22
          IpRanges: 
            - 
              CidrIp: "0.0.0.0/0"
          ToPort: 22
          IpProtocol: "tcp"
          UserIdGroupPairs: []
      GroupName: "{{ env }}-bastion"
      VpcId: "{{ vpc_id }}"
      OwnerId: "{{ aws_custid }}"
      GroupId: "{{ sg_bastion }}"

