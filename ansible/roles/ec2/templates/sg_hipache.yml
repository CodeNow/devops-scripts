---
  SecurityGroups: 
    - 
      IpPermissionsEgress: 
        - 
          IpProtocol: "-1"
          IpRanges: 
            - 
              CidrIp: "0.0.0.0/0"
          UserIdGroupPairs: []
          PrefixListIds: []
      Description: "{{ env }} Hipache Security Policy"
      Tags: 
        - 
          Value: "{{ env }}-hipache"
          Key: "Name"
        - 
          Value: "hipache"
          Key: "Service"
        - 
          Value: "{{ env }}"
          Key: "Environment"
      IpPermissions: 
        - 
          PrefixListIds: []
          FromPort: 80
          IpRanges: 
            - 
              CidrIp: "0.0.0.0/0"
          ToPort: 80
          IpProtocol: "tcp"
          UserIdGroupPairs: []
        - 
          PrefixListIds: []
          FromPort: 32768
          IpRanges: []
          ToPort: 65535
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_hipache }}"
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_web }}"
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_redis }}"
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_services }}"
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_api }}"
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_dock }}"
        - 
          PrefixListIds: []
          FromPort: 22
          IpRanges: []
          ToPort: 22
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_bastion }}"
        - 
          PrefixListIds: []
          FromPort: 8301
          IpRanges: []
          ToPort: 8301
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_hipache }}"
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_services }}"
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_api }}"
        - 
          PrefixListIds: []
          FromPort: 3000
          IpRanges: []
          ToPort: 3000
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_hipache }}"
        - 
          PrefixListIds: []
          FromPort: 443
          IpRanges: 
            - 
              CidrIp: "0.0.0.0/0"
          ToPort: 443
          IpProtocol: "tcp"
          UserIdGroupPairs: []
        - 
          PrefixListIds: []
          FromPort: -1
          IpRanges: 
            - 
              CidrIp: "0.0.0.0/0"
          ToPort: -1
          IpProtocol: "icmp"
          UserIdGroupPairs: []
      GroupName: "{{ env }}-hipache"
      VpcId: "{{ vpc_id }}"
      OwnerId: "{{ aws_custid }}"
      GroupId: "{{ sg_hipache }}"

