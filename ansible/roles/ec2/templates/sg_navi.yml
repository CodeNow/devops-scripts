---
  SecurityGroups: 
    - 
      IpPermissionsEgress: 
        - 
          IpProtocol: "-1"
          IpRanges: 
            - 
              CidrIp: "0.0.0.0/0"
          UserIdGroupPairs: []
          PrefixListIds: []
      Description: "{{ env }} Navi Security Policy"
      Tags: 
        - 
          Value: "{{ env }}-navi"
          Key: "Name"
        - 
          Value: "navi"
          Key: "Service"
        - 
          Value: "{{ env }}"
          Key: "Environment"
      IpPermissions: 
        - 
          PrefixListIds: []
          FromPort: 3567
          IpRanges: []
          ToPort: 3567
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_hipache }}"
        - 
          PrefixListIds: []
          FromPort: 22
          IpRanges: []
          ToPort: 22
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_bastion }}"
      GroupName: "{{ env }}-navi"
      VpcId: "{{ vpc_id }}"
      OwnerId: "{{ aws_custid }}"
      GroupId: "{{ sg_navi }}"

