---
  SecurityGroups: 
    - 
      IpPermissionsEgress: 
        - 
          IpProtocol: "-1"
          IpRanges: 
            - 
              CidrIp: "0.0.0.0/0"
          UserIdGroupPairs: []
          PrefixListIds: []
      Description: "{{ env }} Docks Security Policy"
      Tags: 
        - 
          Value: "{{ env }}-docks"
          Key: "Name"
        - 
          Value: "dock"
          Key: "Service"
        - 
          Value: "{{ env }}"
          Key: "Environment"
      IpPermissions: 
        - 
          PrefixListIds: []
          FromPort: 32768
          IpRanges: []
          ToPort: 65535
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_hipache }}"
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_redis }}"
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_services }}"
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_api }}"
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_dock }}"
        - 
          PrefixListIds: []
          FromPort: 4242
          IpRanges: []
          ToPort: 4242
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_services }}"
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_api }}"
        - 
          PrefixListIds: []
          FromPort: 6783
          IpRanges: []
          ToPort: 6783
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_api }}"
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_dock }}"
        - 
          PrefixListIds: []
          FromPort: 53
          IpRanges: []
          ToPort: 53
          IpProtocol: "udp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_dock }}"
        - 
          PrefixListIds: []
          FromPort: 3112
          IpRanges: []
          ToPort: 3112
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_api }}"
        - 
          PrefixListIds: []
          FromPort: -1
          IpRanges: 
            - 
              CidrIp: "0.0.0.0/0"
          ToPort: -1
          IpProtocol: "icmp"
          UserIdGroupPairs: []
        - 
          PrefixListIds: []
          FromPort: 6783
          IpRanges: []
          ToPort: 6783
          IpProtocol: "udp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_api }}"
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_dock }}"
        - 
          PrefixListIds: []
          FromPort: 8200
          IpRanges: []
          ToPort: 8200
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_services }}"
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_dock }}"
        - 
          PrefixListIds: []
          FromPort: 22
          IpRanges: []
          ToPort: 22
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_bastion }}"
        - 
          PrefixListIds: []
          FromPort: 3200
          IpRanges: []
          ToPort: 3200
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_api }}"
        - 
          PrefixListIds: []
          FromPort: 53
          IpRanges: []
          ToPort: 53
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_dock }}"
        - 
          PrefixListIds: []
          FromPort: 32768
          IpRanges: []
          ToPort: 65335
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_navi }}"
        - 
          PrefixListIds: []
          FromPort: 3100
          IpRanges: []
          ToPort: 3100
          IpProtocol: "tcp"
          UserIdGroupPairs: 
            - 
              UserId: "{{ aws_custid }}"
              GroupId: "{{ sg_api }}"
      GroupName: "{{ env }}-docks"
      VpcId: "{{ vpc_id }}"
      OwnerId: "{{ aws_custid }}"
      GroupId: "{{ sg_dock }}"

