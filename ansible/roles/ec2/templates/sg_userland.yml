---
  SecurityGroups: 
    - 
      IpPermissionsEgress: 
        - 
          IpProtocol: "-1"
          IpRanges: 
            - 
              CidrIp: "0.0.0.0/0"
          UserIdGroupPairs: []
          PrefixListIds: []
      Description: "{{ env }} Userland Hipache Security Policy"
      Tags: 
        - 
          Value: "userland"
          Key: "Service"
        - 
          Value: "{{ env }}-userland"
          Key: "Name"
        - 
          Value: "{{ env }}"
          Key: "Environment"
      IpPermissions: 
        - 
          PrefixListIds: []
          FromPort: 80
          IpRanges: 
            - 
              CidrIp: "0.0.0.0/0"
          ToPort: 65535
          IpProtocol: "tcp"
          UserIdGroupPairs: []
      GroupName: "{{ env }}-userland"
      VpcId: "{{ vpc_id }}"
      OwnerId: "{{ aws_custid }}"
      GroupId: "{{ sg_userland }}"

