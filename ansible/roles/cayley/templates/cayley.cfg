{
  "database": "mongo",
  "db_path": "mongodb://cayley:VM9K8iw2(BiW6F9GvtWv@{% for host in groups['mongodb'] %}{{ hostvars[host]['ansible_default_ipv4']['address'] }}:27000{% if not loop.last %},{% endif %}{% endfor %}/cayley",
  "read_only": false,
  "database_options": {
    "database_name": "cayley"
  }
}
