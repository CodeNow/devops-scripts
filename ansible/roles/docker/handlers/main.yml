---
# handlers file for docker-centos

- name: start docker
  service: name=docker
           state=started
           enabled=yes

- name: restart docker
  sudo: yes
  service: name=docker
           state=restarted
           enabled=yes