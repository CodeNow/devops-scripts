---
- name: check keys
  command: sudo docker run --rm redis redis-cli -h {{redis_host}} LLEN {{replace_key}}
  register: key_length

- name: create key if not exist
  command: sudo docker run --rm redis redis-cli -h {{redis_host}} RPUSH {{replace_key}} {{name}} {{name}}
  when: key_length.stdout == "0"