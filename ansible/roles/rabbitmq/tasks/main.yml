---
# commands to build an image
- name: create build folder
  sudo: yes
  file:
    path="{{ config_path }}"
    state=directory

- name: copy dockerfile to build folder
  sudo: yes
  template:
    src="rabbitmq.config"
    dest="{{ config_path }}"
  tags: 'genConfig'
