apiVersion: batch/v2alpha1
kind: CronJob
metadata:
  name: {{ item.cron_queue }}
spec:
  schedule: "{{ item.cron_scedule }}"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: {{ item.cron_queue }}
            image: {{ container_image }}:{{ container_tag }}
            args: /khronos/bin/cli.js --queue {{ item.cron_queue }} --job '{}' --host {{ cron_rabbit_host_address }} {{ cron_rabbit_auth }}
          restartPolicy: OnFailure
