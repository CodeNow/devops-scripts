---
- name: npm install {{ app_name }}
  sudo: yes
  npm: path=/opt/runnable/{{ app_name }}

- name: restart service {{ app_name }}
  sudo: yes
  service:
    name={{ app_name }}
    state=restarted
    enabled=yes
