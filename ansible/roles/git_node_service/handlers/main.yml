---
- name: npm install {{ app_name }}
  npm:
    path=/opt/runnable/{{ app_name }}
    production=yes

- name: restart service {{ app_name }}
  sudo: yes
  service:
    name={{ app_name }}
    state=restarted
    enabled=yes
