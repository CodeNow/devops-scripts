---
- name: send done message
  slack:
    token={{ slack_token }}
    channel={{ item }}
    msg="{{ notify_end_message }}"
  with_items:
    - "{{ slack_channels }}"
    - "{{ extra_channels | default([]) }}"

