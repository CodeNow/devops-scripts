# message to say when starting deploy
deploy_message: "starting to deploy {{container_image|default(app_name)}} to {{ansible_hostname}}"
# message printed when deploy finished
notify_end_message: "starting to deploy {{container_image|default(app_name)}} to {{ansible_hostname}}"
