---
- name: send start message
  slack:
    token={{ slack_token }}
    channel={{ item }}
    msg="{{ notify_start_message }}"
  with_items:
    - "{{ slack_channels }}"
    - "{{ extra_channels | default([]) }}"
  notify:
    - send done message
