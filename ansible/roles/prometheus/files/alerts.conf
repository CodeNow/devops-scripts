ALERT HookDockUnresponsive
  IF up == 0
  FOR 10m
  LABELS {
    reportTo = "drake",
    type = "unresponsive"
  }
  ANNOTATIONS {
    summary = "Dock unresponsive host={{ $labels.hostIp }} org={{ $labels.org }}",
    description = "Dock unresponsive host={{ $labels.hostIp }} org={{ $labels.org }}"
  }

ALERT DockUnresponsive
  IF up == 0
  FOR 1h
  LABELS {
    reportTo = "pagerduty"
  }
  ANNOTATIONS {
    summary = "Dock unresponsive host={{ $labels.hostIp }} org={{ $labels.org }}",
    description = "Dock unresponsive host={{ $labels.hostIp }} org={{ $labels.org }"
  }

ALERT HookDockDockerDiskFull
  IF (node_filesystem_size{device="/dev/xvdb"} - node_filesystem_free{device="/dev/xvdb"}) / node_filesystem_size{device="/dev/xvdb"}  * 100 > 70
  FOR 5m
  LABELS {
    reportTo = "drake",
    type = "disk_filled"
  }
  ANNOTATIONS {
    summary = "Dock /docker disk 75% host={{ $labels.hostIp }} org={{ $labels.org }}",
    description = "Dock /docker disk 75% host={{ $labels.hostIp }} org={{ $labels.org }}"
  }

ALERT DockDockerDiskFull
  IF (node_filesystem_size{device="/dev/xvdb"} - node_filesystem_free{device="/dev/xvdb"}) / node_filesystem_size{device="/dev/xvdb"}  * 100 > 70
  FOR 30m
  LABELS {
    reportTo = "pagerduty"
  }
  ANNOTATIONS {
    summary = "Dock /docker disk 75% host={{ $labels.hostIp }} org={{ $labels.org }}",
    description = "Playbook here: https://github.com/CodeNow/devops-scripts/wiki/server-out-of-disk"
  }

ALERT DockRootDiskFull
  IF (node_filesystem_size{device="/dev/xvda1"} - node_filesystem_free{device="/dev/xvda1"}) / node_filesystem_size{device="/dev/xvda1"}  * 100 > 75
  FOR 5m
  LABELS {
    reportTo = "pagerduty"
  }
  ANNOTATIONS {
    summary = "Dock root disk 75% host={{ $labels.hostIp }} org={{ $labels.org }}",
    description = "Playbook here: https://github.com/CodeNow/devops-scripts/wiki/server-out-of-disk"
  }

ALERT HookDockOutOfRam
  IF (node_memory_MemFree + node_memory_Buffers + node_memory_Cached) < 100000000
  FOR 5m
  LABELS {
    reportTo = "drake",
    type = "memory_exhausted"
  }
  ANNOTATIONS {
    summary = "Dock out of ram host={{ $labels.hostIp }} org={{ $labels.org }}",
    description = "Dock out of ram host={{ $labels.hostIp }} org={{ $labels.org }}"
  }

ALERT DockOutOfRam
  IF (node_memory_MemFree + node_memory_Buffers + node_memory_Cached) < 100000000
  FOR 30m
  LABELS {
    reportTo = "pagerduty"
  }
  ANNOTATIONS {
    summary = "Dock out of ram host={{ $labels.hostIp }} org={{ $labels.org }}",
    description = "unhealthy dock {{ $labels.hostIp } using dock-cli and message slack #customer channel with org={{ $labels.org }}"
  }

ALERT DockHighLoad
  IF node_load15 > 90
  FOR 30m
  LABELS {
    reportTo = "pagerduty"
  }
  ANNOTATIONS {
    summary = "Dock is experiencing high load host={{ $labels.hostIp }} org={{ $labels.org }}",
    description = "ssh {{ $labels.hostIp }} into dock make sure it is responsive, if it is not, unhealthy. `docks unhealthy -e delta {{ $labels.hostIp }}`"
  }
