---
- name: create daemon folder
  file:
    state: directory
    path: "{{ daemon_sets_path }}"

- name: create registry deamon set
  template:
    dest: "{{ daemon_sets_path }}/{{ name }}"
    src: registry.yml
