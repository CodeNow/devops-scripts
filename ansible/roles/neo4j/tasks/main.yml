---
- name: create config directory
  sudo: yes
  file: path=/opt/neo4j/conf state=directory

- name: copy neo4j config
  sudo: yes
  template:
    src=neo4j-server.properties
    dest=/opt/neo4j/conf/

- name: copy neo4j start.sh
  sudo: yes
  template:
    src=start.sh
    dest=/opt/neo4j/
