---
- hosts: api
  vars_files:
    - group_vars/alpha-image-builder.yml
  roles:
  - { role: builder }

- hosts: "{{ dock | default('docks') }}"
  vars_files:
    - group_vars/alpha-image-builder.yml
  roles:
  - { role: pull-image }
  - { role: consul_value, tags: [consul_value] }
