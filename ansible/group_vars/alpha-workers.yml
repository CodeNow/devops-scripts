name: api-worker

container_image: "{{ registry_host }}/runnable/{{ name }}"

memory_hard_limit: 1g

envs:
  - name: IS_QUEUE_WORKER
    value: "true"
  - name: ROLLBAR_KEY
    value: "{{ api_workers_rollbar_key }}"

container_envs: "{{ api_base_container_envs + envs }}"
