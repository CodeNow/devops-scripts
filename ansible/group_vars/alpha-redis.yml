name: redis
hosted_ports: ["{{ redis_port }}"]

storage_size_gb: 50

volume_mounts:
  - name: "{{ name }}-db-claim"
    path: /data
    kind: persistent

container_image: redis
container_tag: "3.0.5"
container_run_args: ''
