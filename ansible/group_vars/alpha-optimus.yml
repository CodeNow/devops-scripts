name: "optimus"

container_image: "registry.runnable.com/runnable/{{ name }}"
repo: "git@github.com:CodeNow/{{ name }}.git"
hosted_ports: ["80"]
node_version: "0.12.4"
npm_version: "2.8.3"

# for redis
redis_key: "frontend:{{ name }}.{{ domain }}"
is_redis_update_required: 'yes'

container_envs: >
  -h {{ name }}
  -e NODE_ENV={{ node_env }}
  -e DATADOG_HOST={{ datadog_host }}
  -e DATADOG_PORT={{ datadog_port }}
  -e PORT={{ hosted_ports[0] }}
  -e DEBUG=optimus*
  -e S3_CONTEXT_RESOURCE_BUCKET=runnable.context.resources.production
  -e AWS_ACCESS_KEY_ID=AKIAJWSSSJYUXKNW2ZDA
  -e AWS_SECRET_ACCESS_KEY=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

container_run_opts: "-d -P {{container_envs}}"
