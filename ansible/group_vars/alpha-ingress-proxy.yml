---
name: nginx

container_image: "{{ name }}"
container_tag: "1.10"
hosted_ports: ["80", "443"]

service_type: "LoadBalancer"

volume_mounts:
 - name: "{{ name }}-{{ domain }}-certs"
   path: /etc/ssl/certs/{{ domain }}
 - name: "{{ name }}-base-conf"
   path: /etc/nginx/
 - name: "{{ name }}-mixpanel-config"
   path: /etc/nginx/other-sites-enabled
 - name: "{{ name }}-api-config"
   path: /etc/nginx/sites-enabled
