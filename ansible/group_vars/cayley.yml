# container_kill_start settings
container_image: bkendall/cayley
container_tag: "latest"
container_run_opts: "-d -p 64210 -v /opt/cayley:/opt/cayley:r"

# for redis
redis_key: "frontend:cayley.{{ domain }}"
is_redis_update_required: 'yes'
name: "cayley"
