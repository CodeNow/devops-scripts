name: neo4j
hosted_ports: [ 7473, 7474 ]

# container settings
container_run_opts: >
  -d
  -p 7473:7473
  -p 7474:7474
  -v /neo4j:/neo4j-data:rw
  -v /opt/neo4j/conf/neo4j-server.properties:/neo4j/conf/neo4j-server.properties
  -v /opt/neo4j/start.sh:/neo4j/start.sh
  -v /etc/ssl/certs/{{ domain }}:/etc/ssl/certs/{{ domain }}
  -v /etc/ssl/private/{{ domain }}.der:/etc/ssl/private/{{ domain }}.der

# for build
dockerfile: neo4j/Dockerfile
container_image: registry.runnable.com/runnable/neo4j
container_tag: latest
