# krain options
name: krain

krain_env: default

# upstart template variables
app_name: krain
app_repo: git@github.com:CodeNow/krain.git

enviroment_vars: {}

# consul values
consul_values:
  - key: "{{ name }}/version"
    value: "{{ git_branch }}"
