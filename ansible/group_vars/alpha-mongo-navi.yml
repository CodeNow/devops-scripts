# database vars
name: mongo-navi

db_path: /opt/mongodb/db

# container_kill_start settings
container_image: mongo
container_tag: 3.2.4
container_run_opts: >
  -h {{ name }}
  -d
  -p {{ navi_mongo_port }}:27017
  -v {{ db_path }}:/data/db:rw
