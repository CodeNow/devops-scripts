name: mongo-navi

storage_size_gb: 50

volume_mounts:
  - name: "{{ name }}"
    path: /data/db
    kind: persistent

container_image: mongo
container_tag: "3.2.6"

hosted_ports: ["{{ mongo_navi_port }}"]
