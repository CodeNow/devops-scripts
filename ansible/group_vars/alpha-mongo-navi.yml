name: mongo-navi
hosted_ports: ["{{ mongo_navi_port }}"]

storage_size_gb: 50

volume_mounts:
  - name: "{{ name }}-db-claim"
    path: /data/db
    kind: persistent

container_image: mongo
container_tag: "3.2.6"
