name: "navi"

container_image: "registry.runnable.com/runnable/navi"
container_tag: "latest"
dockerfile: "basic_node/Dockerfile"
repo: "git@github.com:CodeNow/navi.git"
hosted_port: "3567"
node_version: "0.12.0"
npm_version: "2.1.18"

container_envs: >
  -h navi
  -e NODE_ENV={{ node_env }}
  -e DATADOG_HOST={{ datadog_host }}
  -e DATADOG_PORT={{ datadog_port }}
  -e DEBUG=*

container_run_opts: "-d -p {{hosted_port}}:{{hosted_port}} {{container_envs}}"
