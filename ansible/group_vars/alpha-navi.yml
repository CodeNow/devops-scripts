name: "navi"

container_image: "registry.runnable.com/runnable/{{ name }}"
repo: "git@github.com:CodeNow/{{ name }}.git"
hosted_ports: ["3567"]
node_version: "0.12.0"
npm_version: "2.1.18"

container_envs: >
  -h navi
  -e NODE_ENV={{ node_env }}
  -e DATADOG_HOST={{ datadog_host }}
  -e DATADOG_PORT={{ datadog_port }}
  -e DEBUG=*
  -e HTTP_PORT={{ hosted_ports[0] }}
  -e REDIS_IPADDRESS={{redis_host}}
  -e HELLO_RUNNABLE_GITHUB_TOKEN=0e55b73c0146f033e361ed327d088d30e2804869

container_run_opts: "-d -p {{hosted_ports[0]}}:{{hosted_ports[0]}} {{container_envs}}"
