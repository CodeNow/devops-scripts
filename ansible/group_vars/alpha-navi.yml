name: "navi"

container_image: "registry.runnable.com/runnable/{{ name }}"
container_tag: "{{ git_branch }}"
repo: "git@github.com:CodeNow/{{ name }}.git"
hosted_ports: ["3567"]
node_version: "4.1.0"
npm_version: "2.8.3"

redis_host: "{{ hostvars[groups['redis'][0]]['ansible_default_ipv4']['address'] }}"
redis_port: 6379

mongo_host: "{{ hostvars[groups['mongo-navi'][0]]['ansible_default_ipv4']['address'] }}"

container_envs: >
  -e DATADOG_HOST={{ datadog_host }}
  -e DATADOG_PORT={{ datadog_port }}
  -e ERROR_URL=http://{{ hostvars[groups['detention'][0]]['ansible_default_ipv4']['address'] }}:9123
  -e HTTP_PORT={{ hosted_ports[0] }}
  -e MONGO=mongodb://{{ mongo_host }}:27017/{{ navi_mongo_database }}
  -e NODE_ENV={{ node_env }}
  -e REDIS_IPADDRESS={{redis_host}}

container_run_opts: "-h navi -d -p {{hosted_ports[0]}}:{{hosted_ports[0]}} {{container_envs}}"
