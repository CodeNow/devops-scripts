name: drake-http

container_image: registry.runnable.com/runnable/{{ name }}
container_tag: "{{ git_branch }}"
hosted_ports: ["{{ drake_port }}"]
repo: "git@github.com:CodeNow/drake.git"
node_version: lts
npm_version: 2

# Exposes drake (heh)
redis_key: "frontend:{{ drake_hostname }}"
is_redis_update_required: 'yes'

# for container settings
dockerfile_environment: |
  ENV API_URL {{ api_url }}
  ENV DATADOG_HOST {{ datadog_host_address }}
  ENV DATADOG_PORT {{ datadog_port }}
  ENV DATADOG_SECRET I_solemnly_swear_that_I_am_up_to_no_good
  ENV NODE_ENV {{ node_env }}
  ENV PORT {{ hosted_ports[0] }}
  ENV RABBITMQ_HOSTNAME {{ rabbit_host_address }}
  ENV RABBITMQ_PASSWORD {{ rabbit_password }}
  ENV RABBITMQ_PORT {{ rabbit_port }}
  ENV RABBITMQ_USERNAME {{ rabbit_username }}
  ENV ROLLBAR_KEY {{ drake_http_rollbar_token }}

container_run_opts: >
  -h {{ name }}
  -d
  -P

npm_start_command: run start-http
