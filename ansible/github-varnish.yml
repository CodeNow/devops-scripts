---
- hosts: consul

- hosts: builder
  vars_files:
  - group_vars/alpha-github-varnish.yml
  roles:
  - { role: build_with_dockerfile, tags: [ build ] }
  - { role: push, tags: [ push ] }

- include: aws/github-varnish.yml
  tags: [ lc ]
- include: deploy/github-varnish.yml
  tags: [ deploy ]
