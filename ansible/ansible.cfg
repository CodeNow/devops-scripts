[defaults]
# Required so `sudo: yes` does not lose the environment variables, which hold the ssh-agent socket
sudo_flags=-HE
gathering = smart
fact_caching = jsonfile
fact_caching_connection = _cache
fact_caching_timeout = 86400
