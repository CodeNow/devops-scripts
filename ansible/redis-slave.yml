---
- hosts: redis-slave
  vars_files:
    - "group_vars/alpha-redis.yml"
  roles:
  - { role: notify, tags: "notify" }
  - { role: redis_slave, tags: "deploy" }
  - { role: container_kill_start,
      container_run_args: "redis-server --slaveof {{ redis_host }}",
      tags: "deploy" }
