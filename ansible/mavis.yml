---
- hosts: logstash-broker

- hosts: mavis
  vars_files:
    - "group_vars/alpha-mavis.yml"
  roles:
  - { role: notify, tags: "notify" }
  - { role: redis_key, tags: ["setup", "redis_key"] }
  - { role: builder, tags: "build" }
  - { role: container_start, tags: "deploy" }
