---
- hosts: charon
- hosts: redis
- hosts: redis-slave
- hosts: neo4j
- hosts: mongodb

- hosts: localhost
  roles:
  - { role: update-local-inventory}

- hosts: "{{ host_name }}"
  vars_files:
    - "group_vars/alpha-docks.yml"
  roles:
    # - { role: provision, when: provision is defined, tags: "provision"}
    - { role: dock, tags: "setup" }
    - { role: docker, tags: "docker" }
    - { role: iptables, tags: "iptables, security" }
