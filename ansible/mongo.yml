---
- hosts: mongodb
  vars_files:
    - group_vars/alpha-mongo.yml
  roles:
  - { role: notify, tags: [ notify ] }
  - { role: database }
  - { role: mongo-server, tags: [ tls ] }
  - { role: container_kill_start }
  - { role: datadog, tags: [ datadog ] }
