---
- hosts: mongodb
  vars_files:
    - "group_vars/alpha-mongo.yml"
  roles:
  - { role: notify, tags: "notify" }
  - { role: database }
  - { role: container_kill_start, tags: "deploy" }
  - { role: datadog }
