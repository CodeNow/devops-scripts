---
- hosts: mongo-navi
- hosts: rabbitmq

- hosts: link
  vars_files:
    - "group_vars/alpha-link.yml"
  roles:
  - { role: notify, tags: "notify" }
  - { role: builder, tags: "build" }
  - { role: container_kill_start }
