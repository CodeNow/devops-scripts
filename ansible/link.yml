---
- hosts: mongo-navi
- hosts: rabbitmq
- hosts: consul
- hosts: link

- hosts: builder
  vars_files:
    - "group_vars/alpha-link.yml"
  roles:
  - { role: notify, tags: "notify" }
  - { role: builder, tags: "build" }

- include: aws/link.yml
- include: deploy/link.yml
