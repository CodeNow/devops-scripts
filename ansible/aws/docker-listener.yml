---
- hosts: docker-listener

- hosts: ec2
  vars_files:
    - ../group_vars/alpha-docker-listener.yml
  connection: local
  gather_facts: false
  roles:
  - { role: ../roles/create_lc, tags: [ lc ] }
