---
- hosts: big-poppa
- hosts: cream
- hosts: consul
- hosts: datadog
- hosts: rabbitmq

- hosts: builder
  vars_files:
    - group_vars/alpha-cream-worker.yml
  roles:
  - { role: notify, rollbar_token: "{{ cream_http_rollbar_token }}", tags: [ notify ] }
  - { role: builder }
  - { role: push }

- include: aws/cream-worker.yml
- include: deploy/cream-worker.yml
