---
- hosts: rabbitmq
- hosts: consul

- hosts: builder
  vars_files:
    - group_vars/alpha-cream-worker.yml
  roles:
  - { role: notify, rollbar_token: "{{ cream_http_rollbar_token }}", tags: [ notify ] }
  - { role: builder, tags: [ build ] }
  - { role: push, tags: [ push ] }

- include: deploy-cream-worker.yml
  tags: [ deploy ]
