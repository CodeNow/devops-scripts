---
- hosts: redis
- hosts: consul

- hosts: builder
  vars_files:
    - group_vars/alpha-optimus.yml
  roles:
  - { role: notify, rollbar_token: "{{ optimus_rollbar_token }}" }
  - { role: builder }
  - { role: push  }

- include: aws/optimus.yml
- include: deploy/optimus.yml
