---
- hosts: redis
- hosts: rabbitmq

- hosts: metis
  vars_files:
    - "group_vars/alpha-metis.yml"
  roles:
  - { role: notify, tags: "notify" }
  - { role: builder, tags: "build" }
  - { role: container_kill_start }
