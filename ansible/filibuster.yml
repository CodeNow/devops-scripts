---
- hosts: docks
  vars_files:
    - "group_vars/alpha-filibuster.yaml"
  tasks:
    - command: sudo docker run --rm -v /usr/local/bin:/target jpetazzo/nsenter
      when: update is defined
  roles:
  - { role: git_node_service, tags: "deploy" }

