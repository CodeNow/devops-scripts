---
- hosts: api
  vars_files:
    - group_vars/alpha-api-base.yml
    - group_vars/alpha-api.yml
  roles:
  - role: datadog
    tags: [ datadog ]
          
  - role: pull_start
    number_of_containers: 1
