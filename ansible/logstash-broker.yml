---
- hosts: logstash-broker
  vars_files:
    - "group_vars/alpha-logstash-broker.yml"
  roles:
  - { role: notify, tags: "notify" }
  - { role: database, tags: "deploy" }
  - { role: container_kill_start, tags: "deploy" }
