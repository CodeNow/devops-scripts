---
- hosts: redis
- hosts: rabbitmq
- hosts: consul

- hosts: shiva
  vars_files:
    - group_vars/alpha-shiva.yml
  roles:
  - role: notify
    rollbar_token: "{{ shiva_rollbar_token }}"
    tags: [ notify ]
  - { role: builder, tags: [ build ] }
  - { role: tls-server-ca, ca_dest: "{{ build_dir }}/{{ name }}/ssl/redis/ca.pem" }
  #- { role: container_kill_start }
