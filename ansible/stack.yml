- include: api.yml git_branch="{{ api_branch }}" --skip-tags build
- include: detention.yml git_branch="{{ detention_branch }}" --skip-tags build
- include: eru.yml git_branch="{{ eru_branch }}" --skip-tags build
- include: khronos.yml git_branch="{{ khronos_branch }}" --skip-tags build
- include: link.yml git_branch="{{ link_branch }}" --skip-tags build
- include: mongo-navi.yml
- include: navi.yml git_branch="{{ navi_branch }}" --skip-tags build
- include: optimus.yml git_branch="{{ optimus_branch }}" --skip-tags build
- include: palantiri.yml git_branch="{{ palantiri_branch }}" --skip-tags build
- include: rabbitmq.yml
- include: redis.yml
- include: registry.yml
- include: sauron.yml git_branch="{{ sauron_branch }}" --skip-tags build
- include: shiva.yml git_branch="{{ astral_branch }}" --skip-tags build
- include: swarm-manager.yml
