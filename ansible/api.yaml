---
- hosts: api
  vars_files:
    - "group_vars/alpha-api.yaml"
  roles:
  - { role: redis_key, tags: ["setup", "redis_key"] }
  - { role: builder, tags: "build" }
  - { role: containerStart, tags: "deploy" }