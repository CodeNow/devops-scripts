apiVersion: v1
kind: ConfigMap
metadata:
  name: ingress-proxy-mixpanel-config
data:
  mixpanel.conf: |
    server {
      listen 80;
      listen [::]:80;
      server_name mixpanel.runnable-beta.com;
      return 301 https://$server_name$request_uri;
    }

    server {
      listen 443 ssl;
      server_name mixpanel.runnable-beta.com;

      ssl on;
      ssl_certificate /etc/ssl/certs/runnable-beta.com/chained.pem;
      ssl_certificate_key /etc/ssl/certs/runnable-beta.com/key.pem;

      location / {
        proxy_pass https://api.mixpanel.com;
      }
    }
