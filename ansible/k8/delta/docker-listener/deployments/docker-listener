apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: docker-listener
spec:
  replicas:  1 
  template:
    metadata:
      labels:
        app: docker-listener
    spec:
      imagePullSecrets:
        - name: runnable-kubernetes-pull-secret
      containers:
        - image: quay.io/runnable/docker-listener:v10.8.3
          imagePullPolicy: Always
          name: docker-listener
          resources:
            requests:
              cpu: "250m"
              memory: "500M"
            limits:
              cpu: "1550m"
              memory: 1500M
          args:
            - npm
            - start
          env:
            - name: CONSUL_HOST
              value: "10.8.6.122:8500"
            - name: DATADOG_HOST
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: DATADOG_PORT
              value: "8125"
            - name: DOCKER_CERT_PATH
              value: "/etc/ssl/docker"
            - name: LOGGLY_TOKEN
              value: "f673760d-e0b3-4a93-a15e-2862ea074f91"
            - name: NODE_ENV
              value: "production-delta"
            - name: RABBITMQ_HOSTNAME
              value: "10.8.4.195"
            - name: RABBITMQ_PASSWORD
              value: "wKK7g7NWKpQXEeSzyWB7mIpxZIL8H2mDSf3Q6czR3Vk"
            - name: RABBITMQ_PORT
              value: "54321"
            - name: RABBITMQ_USERNAME
              value: "o2mdLh9N9Ke2GzhoK8xsruYPhIQFN7iEL44dQJoq7OM"
            - name: ROLLBAR_KEY
              value: "7b5924eccfff415d9fc00c051811e9d7"
            - name: SWARM_HOST
              value: "swarm:2375"
            - name: IMAGE_INSPECT_LIST
              value: "localhost,runnable/image-builder"
          volumeMounts:
            - name: docker-listener-docker-ssl-certs
              mountPath: /etc/ssl/docker
      volumes:
        - name: docker-listener-docker-ssl-certs
          configMap:
            name: docker-listener-docker-ssl-certs
