---
- hosts: hipache
  vars_files:
    - "group_vars/alpha-hipache.yml"
  roles:
  - { role: notify, tags: "notify" }
  - { role: hipache, tags: "deploy" }
  - { role: container_kill_start, tags: "deploy" }
