---
- hosts: consul

- hosts: "{{ host }}"
  vars_files:
  - group_vars/{{ var_file }}
  - group_vars/alpha-consul-template-updater.yml
  roles:
  - role: notify
    tags: notify
  # Adding this at the end of the other thing
  - role: container_kill_start
