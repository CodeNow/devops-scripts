---
- hosts: registry

- hosts: docks
  vars_files:
    - group_vars/sauron.yml
  roles:
  - { role: notify, tags: notify }
  - { role: sauron, tags: notify }
  - { role: git_node_service, tags: deploy }
