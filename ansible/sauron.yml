---
- hosts: rabbitmq
- hosts: consul
- hosts: swarm-manager

- hosts: sauron
  vars_files:
    - group_vars/alpha-sauron.yml
  roles:
  - { role: notify, rollbar_token: "{{ sauron_rollbar_token }}", tags: [ notify ] }
  - { role: docker_client }
  - { role: builder }
  - { role: push  }

- include: aws/sauron.yml
- include: deploy/sauron.yml
