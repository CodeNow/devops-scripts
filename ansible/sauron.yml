---
- hosts: rabbitmq
- hosts: swarm-manager
- hosts: sauron
- hosts: consul

- hosts: builder
  vars_files:
    - group_vars/alpha-sauron.yml
  roles:
  - { role: notify, rollbar_token: "{{ sauron_rollbar_token }}" }
  - { role: docker_client }
  - { role: builder }
  - { role: push  }

- include: aws/sauron.yml
- include: deploy/sauron.yml
