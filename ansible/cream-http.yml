---
- hosts: big-poppa
- hosts: cream
- hosts: consul
- hosts: github-varnish
- hosts: rabbitmq

- hosts: builder
  vars_files:
    - group_vars/alpha-cream-http.yml
  roles:
  - { role: notify, rollbar_token: "{{ cream_http_rollbar_token }}", tags: [ notify ] }
  - { role: builder, tags: [ build ] }
  - { role: push, tags: [ push ] }

- include: deploy-cream-http.yml
  tags: [ deploy ] 
