---
- hosts: mongodb
- hosts: navi
- hosts: rabbitmq
- hosts: redis
- hosts: swarm-manager
- hosts: consul
- hosts: big-poppa
- hosts: cream
- hosts: github-varnish

- hosts: builder
  vars_files:
    - group_vars/alpha-api-base.yml
    - group_vars/alpha-socket-server.yml
  roles:
  - { role: notify, rollbar_token: "{{ api_socket_server_rollbar_key }}", tags: [ notify ] }
  - { role: docker_client }
  - { role: tls-client, tls_service: mongodb }
  - { role: builder }
  - { role: push  }

- include: aws/socket-server.yml
- include: deploy/socket-server.yml
